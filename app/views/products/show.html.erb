<h1><%= @product.name %></h1>
<p><%= @product.description %></p>
<p><%= image_tag @product.picture(:thumb) %></p>
<strong><%= number_to_currency @product.price %></strong>
<% if @product.price? %>
  <%= link_to 'Add to cart', add_product_cart_index_path(@product), method: :post %>
<% end %>
