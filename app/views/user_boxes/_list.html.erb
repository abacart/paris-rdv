<ul>
  <% @user_box.products.each do |p| %>
    <li>
    <%= p.name %>
    </li>
  <% end %>
</ul>

<% if @user_box.full? %>
  <p><%= link_to 'Add to cart',add_box_cart_index_path(@user_box), method: :post, class: "text-success" %></p>
<% end %>
